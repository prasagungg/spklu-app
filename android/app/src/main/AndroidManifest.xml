<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dibutuhkan untuk memanggil REST API SPKLU. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Membaca kartu e-Money di halaman pembayaran. -->
    <uses-permission android:name="android.permission.NFC"/>
    <!-- required="false" supaya aplikasi tetap bisa dipasang di
         perangkat tanpa NFC; halaman pembayaran menjelaskan keadaannya
         sendiri bila pembacanya tidak ada. -->
    <uses-feature android:name="android.hardware.nfc" android:required="false"/>

    <application
        android:label="SPKLU"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- lockTaskMode="if_whitelisted" mengizinkan kunci kiosk saat
             paketnya sudah di-whitelist device owner. Tanpa itu
             startLockTask() hanya memunculkan dialog screen pinning yang
             bisa dibatalkan pengguna. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:lockTaskMode="if_whitelisted"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- HOME + DEFAULT membuat aplikasi ini bisa dipilih
                     sebagai Home app, jadi tombol Home perangkat
                     kembali ke sini alih-alih ke launcher bawaan.
                     LAUNCHER dipertahankan supaya ikonnya tetap ada di
                     daftar aplikasi. -->
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- Sasaran `adb shell dpm set-device-owner`. Tanpa receiver ini
             perangkat tidak bisa di-provision, dan tanpa device owner
             kunci kiosk tidak menyala — aplikasi tetap jalan normal. -->
        <receiver
            android:name=".AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin"/>
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
